
// This main is set up to test the GUI class for now

import java.io.IOException;

public class Main {
    public static void main(String[] args) {
        System.out.println("main was run, do you see a window? ");
        MatrixGUI application = new MatrixGUI();

        // String filePath = application.getFilePath();
        // System.out
        // .println("Line 12 in Main is passing this value = " + filePath + " to the
        // readMatrixFromFile() method");

        // try {
        // MatrixFileProcessor.readMatrixFromFile(filePath);
        // } catch (IOException e) {
        // e.printStackTrace();
        // }
    }
}
